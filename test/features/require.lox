namespace test.features
val filePath = "test/features/Fraction.lox"
require filePath

var frac = Fraction(3, 6)
var frac2 = frac.reduce()
println(frac.toString())
println(frac2.toString())
println(frac == frac2)