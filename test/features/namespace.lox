namespace test.features
using clox.std.collection
using clox.std.util.Date
using clox.std.util.UUID as ID
using examples.lib.Fraction

println(collection)
println(collection.Array)
println(Date)
println(ID)
println(Fraction)

val frac = Fraction(3, 6)
println(frac.toString())
val frac2 = frac.reduce()
println(frac2.toString())