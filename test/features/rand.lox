namespace test.features
using clox.std.util.Random

val rand = Random()
println("Generating random numbers between 0 to 10000: ")
println("Current seed is: ${rand.getSeed()}")

var i = 0
while(i < 100){ 
    i = i + 1
    println("Random number ${i}: ${rand.nextIntBounded(10000)}")
}